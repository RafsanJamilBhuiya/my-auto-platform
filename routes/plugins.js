// Compatibility route for the platform Plugin/Integration Hub.
// The canonical implementation lives in integrations.js and uses the same persistent store.
module.exports = require('./integrations');
